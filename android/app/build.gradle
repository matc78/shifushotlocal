plugins {
    id "com.android.application"
    id "kotlin-android"
    id "com.google.gms.google-services" // FlutterFire Configuration
    id "dev.flutter.flutter-gradle-plugin" // Flutter Gradle Plugin
}

android {
    namespace = "com.example.shifushotlocal"
    compileSdk = 34 // Replace `flutter.compileSdkVersion` with actual value if undefined
    ndkVersion = "23.1.7779620" // Replace `flutter.ndkVersion` with the correct version or remove if not needed

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId = "com.example.shifushotlocal" // Unique Application ID
        minSdk = 23 // Replace `flutter.minSdkVersion` with actual value
        targetSdk = 34 // Replace `flutter.targetSdkVersion` with actual value
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug // Replace with your signing config
            minifyEnabled false
            shrinkResources false
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation "androidx.core:core-ktx:1.10.1"
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.8.21"
}
